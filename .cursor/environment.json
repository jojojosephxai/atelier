{
  "name": "Atelier",
  "install": "npm ci && npm run build",
  "ports": [{ "name": "web", "port": 8080 }],
  "terminals": [
    {
      "name": "web",
      "command": "npm run preview:8080",
      "description": "Production preview on 8080 (works in Cursor Preview; dev HMR often does not)"
    }
  ]
}
