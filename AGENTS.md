# Atelier

OG pending marker: `/workspace/.grok/og-pending`. Treat it as stale after 10 minutes.

6. **Brand-asset pass.** Never wait_tasks. Never get_task_output.

7. **Verify.** Run the app checks. Do not wait on the brand task.
